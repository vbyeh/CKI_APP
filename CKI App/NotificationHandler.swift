import Foundation
import UIKit

class NotificationHandler: UIViewController{    //when it doesn't conform to protocol it is because some functions need to be implemented
    override func viewDidLoad() {
        super.viewDidLoad()
    }
}
